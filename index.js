// arrays, objetos

let meta = {
  value: " ler um livro por mês",
  address: 2,
  checKed: true,
  log: (info) => {
    console.log(info);
  }        
}
meta.value = "Não é mais ler um livro"
meta.log(meta.value)

// fubction // arrays
// const criarMetas = () => {}

// function criarMetas(params) {}